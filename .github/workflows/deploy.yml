name: Auto Deploy

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Run deploy script
        run: |
          chmod +x scripts/deploy.sh || true
          ./scripts/deploy.sh || true
