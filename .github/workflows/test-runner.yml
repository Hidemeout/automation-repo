name: Test Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Print system info
        run: |
          echo "Runner работает! Host: $(hostname)"
          uname -a
          whoami
          df -h
          free -m
